# Energy Agent Dashboard - Environment Variables

# =============================================================================
# OpenAI API Configuration
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# AI Model to use (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-3.5-turbo
AI_MODEL=gpt-4o-mini

# =============================================================================
# AI Usage Limits (Cost Control)
# =============================================================================
# Maximum AI usage per day (in minutes of API time)
# This prevents excessive API costs
MAX_DAILY_MINUTES=20

# Alternative: Set in seconds (MAX_DAILY_MINUTES * 60)
# MAX_DAILY_SECONDS=1200

# =============================================================================
# Scraping Configuration
# =============================================================================
# How often to run automatic scraping (in hours)
SCRAPE_INTERVAL_HOURS=12

# Whether to fetch full article content (slower but better AI summaries)
# Set to 'false' to use only snippets (faster, cheaper)
FETCH_ARTICLE_CONTENT=true

# =============================================================================
# AI Search Settings
# =============================================================================
# Enable AI-powered smart search and topic discovery
# Set to 'true' to enable AI features, 'false' to use basic keyword matching
AI_SEARCH_ENABLED=false

# =============================================================================
# Setup Instructions
# =============================================================================
# 1. Copy this file as .env:
#    cp .env.example .env
#
# 2. Replace 'your-openai-api-key-here' with your actual OpenAI API key
#
# 3. Adjust other settings as needed:
#    - Increase MAX_DAILY_MINUTES if you need more AI processing
#    - Set FETCH_ARTICLE_CONTENT=false for faster scraping
#    - Set AI_SEARCH_ENABLED=true to enable smart features
#
# 4. Never commit the .env file to version control!
#
# =============================================================================
# Cost Estimation
# =============================================================================
# GPT-4o-mini pricing:
# - Input: $0.150 / 1M tokens (~$0.0002 per request)
# - Output: $0.600 / 1M tokens (~$0.0008 per request)
#
# With MAX_DAILY_MINUTES=20:
# - Approximately 60-100 AI summaries per day
# - Estimated daily cost: $0.02 - $0.10
# - Monthly cost: $0.60 - $3.00
#
# =============================================================================
